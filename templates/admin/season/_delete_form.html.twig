<form method="post" action="{{ path('season_delete', {'id': season.id}) }}">
  <input type="hidden" name="_method" value="DELETE">
  <button type="button" data-toggle="modal" data-target="#deleteModal{{ season.id }}" class="btn btn-outline-danger">
    <i
      class="far fa-trash-alt"></i></button>
</form>

<!-- Modal -->
<div class="modal fade" id="deleteModal{{ season.id }}" tabindex="-1" role="dialog" aria-labelledby="modelTitleId"
     aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title">Suppression</h4>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true"><i class="fas fa-times"></i></span>
        </button>
      </div>
      <div class="modal-body" style="font-size: 20px">
        Êtes-vous sûr de vouloir supprimer la saison {{ season.number }} année {{ season.year }} de {{ season.program.slug | replace({'-' : ' '}) | title }}?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Non</button>
        <form method="post" action="{{ path('season_delete', {'id': season.id}) }}">
          <input type="hidden" name="_method" value="DELETE">
          <button type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}"
                  class="btn btn-danger">Oui
          </button>
        </form>
      </div>
    </div>
  </div>
</div>
