{% extends 'base.html.twig' %}

{% block title %}

  Modifier la série : {{ program.slug | replace({'-' : ' '}) | title }}

{% endblock %}

{% block body %}

  <div class="wrapperProgramEdit container">

    <h1>Modifier la série : {{ program.slug | replace({'-' : ' '}) | title }}</h1>
    {{ include('admin/program/_form.html.twig', {'button_label': 'Mise à jour'}) }}
    <div class="row text-left">
      <div class="col">
        <a href="{{ path('program_index') }}">Retour à la liste</a>
      </div>
    </div>

  </div>

{% endblock %}
