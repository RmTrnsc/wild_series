{{ form_start(form) }}
<div class="container">
  <div class="row align-items-center">
    <div class="col-6">
      {{ form_label(form.picture, 'Url de l\'image') }}
      {{ form_widget(form.picture) }}
    </div>
      <div class="col-6">
        <img src="{{ image.picture }}" alt="{{ image.id }}">
      </div>
  </div>
  <div class="row mt-3">
    <div class="col">
      {{ form_label(form.episodePicture, 'Épisode de l\'image') }}
      {{ form_widget(form.episodePicture) }}
    </div>
  </div>
  <div class="row text-center mt-3">
    <div class="col">
      <button class="btn btn-outline-dark">{{ button_label|default('Enregistrer') }}</button>
    </div>
  </div>
</div>
{{ form_end(form) }}
