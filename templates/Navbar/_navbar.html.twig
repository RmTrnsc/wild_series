<nav class="navbar navbar-expand-lg fixed-top navbar-light bg-light m-3 align-items-center">
  {% if is_granted("ROLE_ADMIN") %}
    <a class="navbar-brand" href="{{ path('app_index') }}" target="_blank">Accueil</a>
  {% else %}
    <a class="navbar-brand" href="{{ path('app_index') }}">Accueil</a>
  {% endif %}
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse align-items-center" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto align-items-center">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('wild_index') }}">Séries</a>
      </li>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('show_categories') }}">Catégories</a>
      </li>
      <li>
        <a class="nav-link" href="{{ path('actor') }}">Les acteurs</a>
      </li>

      {% if is_granted("ROLE_ADMIN") %}

        <div class="adminLink d-flex align-items-center">

          <li class="nav-item">
            <p>Administration :</p>
          </li>
          <li class="nav-item d-flex align-items-center">
            <a class="nav-link" href="{{ path('program_index') }}">Séries</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('category_index') }}">Catégories</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('season_index') }}">Saisons</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('episode_index') }}">Épisodes</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('image_index') }}">Images</a>
          </li>

        </div>
      {% endif %}

    </ul>

    {% if is_granted("ROLE_ADMIN") %}
      <a href="{{ path('app_logout') }}" class="btn btn-outline-danger ml-4">Déconnexion
      </a>
    {% endif %}

  </div>
</nav>
