{% extends 'base.html.twig' %}

{% block body %}

  <div class="wrapperSlug container text-center">
    <h1 class="text-center mt-5">{{ pageTitle }}</h1>
    <h2 class="text-center mt-5">Slug : {{ slug }}</h2>
    <a href="{{ path('wild_index') }}">Return wild series page</a>
  </div>

{% endblock %}